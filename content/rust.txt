println!("Hello, {}!", user_name);
let mut buffer = String::new(); io::stdin().read_line(&mut buffer)?;
match result { Ok(val) => println!("{}", val), Err(e) => panic!(e) }
pub fn get_hex_value(input: i32) -> String { format!("{:X}", input) }
let v: Vec<i32> = vec![1, 2, 3].iter().map(|x| x * 2).collect();
